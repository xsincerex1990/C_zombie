#include "lib/utils.h"
#include "lib/macros.h"
#include "lib/connect.h"

int main(void) {
  char* name = //get clients username and store it in name
  int channel = // initiate a channel give SERVER, PORT, and name;
  //Allocate stack space of size CMD_LENGTH to hold data of type char. Call the stack pointer msg 
  printf("%s joining the botnet\n", name);

  /*
  infiniteLoop {
    receive(..);
    parse(..);
  }
  */
}